# Transition-era DA2 wrappers

These files were the intermediate DA2 entrypoints used during the refactor
before the project converged on:

```bash
python scripts/train.py --config ...
python scripts/eval.py --config ...
```

They are preserved only to make old notes and parity commands interpretable.
New runs should use the canonical unified entrypoints instead.
