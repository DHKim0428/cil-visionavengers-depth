# Legacy DA3 / GeoCalib artifacts

This directory preserves small historical diagnostic artifacts that previously sat
in the repository root during the augmentation / teacher-mask investigation.
They are not canonical training inputs; new generated artifacts should live in
scratch instead of the source tree.
