# Legacy U-Net SLURM wrappers

These wrappers target the original root-level `train.py` path and are preserved
for reproduction only.  New runs should use `scripts/slurm/train.sbatch` with a
canonical experiment config.
