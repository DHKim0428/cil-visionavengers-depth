# Legacy Vast.ai U-Net wrappers

These wrappers target the original root-level `train.py` U-Net baseline that was
superseded by the unified trainer in R10.  They are kept only for historical
reproduction; new experiments should use `scripts/train.py` with a U-Net config.
