name: tilt_geometry
paired_spatial:
  hflip:
    enabled: false
  rotation:
    enabled: false
  crop:
    enabled: false
  tilt:
    mode: geometry
    prob: 0.5
    max_yaw_deg: 5.0
    max_pitch_deg: 5.0
    fov_deg: 60.0
rgb_only:
  color_jitter:
    enabled: false
